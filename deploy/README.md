# ? Linux ?????? LSB-TI

??**????**?`index.html`?`app.js`?`style.css`??? `assets/sprites/*.png`??? Node???????

---

## ????Docker????

?**??? CI** ???????????????????? `git clone` ????

```bash
# 1. ????????? Dockerfile ????
cd /path/to/linux-sbti

# 2. ??
docker build -t linux-sbti .

# 3. ?????? 8080 ????? 80?
docker run -d --restart=unless-stopped -p 8080:80 --name linux-sbti linux-sbti
```

??????`http://?????IP:8080/`????????????? 8080?

?????????? **Caddy / Nginx ?? + HTTPS**??????????? `127.0.0.1:8080`?

```bash
docker run -d --restart=unless-stopped -p 127.0.0.1:8080:80 --name linux-sbti linux-sbti
```

????? `assets/sprites/`??????? `reference-16personalities.png`?????????

---

## ????????? Nginx ?????? Docker?

1. ?????????????????????????????

   - `index.html`
   - `app.js`
   - `style.css`
   - ?? `assets/sprites/` ????? `.png`?

2. ??????? Debian/Ubuntu ????? Nginx?

   ```bash
   sudo apt update && sudo apt install -y nginx
   ```

3. ??????? `/var/www/linux-sbti/`?Nginx ???????

   ```nginx
   server {
       listen 80;
       server_name ?????IP;
       root /var/www/linux-sbti;
       index index.html;
       location / {
           try_files $uri $uri/ /index.html;
       }
   }
   ```

4. ?? Nginx?

   ```bash
   sudo nginx -t && sudo systemctl reload nginx
   ```

5. ???? `scp` ? `rsync` ?????

   ```bash
   rsync -avz --delete ./linux-sbti/ ??@???:/var/www/linux-sbti/
   ```

---

## HTTPS ?????????

- ? **Caddy** ? **Nginx** ?? 443??? Let?s Encrypt ?????? `http://127.0.0.1:8080`??? Docker???? `root` ????????? Docker??

---

## ?????

???? `https://example.com/toys/linux-sbti/`?? `index.html` ? `<head>` ???????????

```html
<base href="/toys/linux-sbti/" />
```

`app.js` ????? `assets/sprites/...` ?????? `<base>` ???

---

## ??

???????????????? **PNG ??**???? 404???? emoji?
